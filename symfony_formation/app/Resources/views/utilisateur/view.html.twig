{% extends 'base.html.twig' %}

{% block menu %}
{# surcharge du block menu dans base.html.twig : on laisse le block vide
    donc plus de menu sur ce template
#}
{% endblock %}

{% block body %}
    {{ utilisateur.getEmail() }}<br>
    {{ article.getDateNaissance() }}<br>
     {{ article.getPassword() }}<br>
    Publié le {{ article.getCreatedAt() | date('d/m/Y à H:i:s') }}
{% endblock %}